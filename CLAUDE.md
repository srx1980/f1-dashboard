AGENTS.md — F1 Dashboard

> Read this first. This file tells you (Cursor) everything you need to know to build the frontend for the F1 Dashboard. No guessing, no assumptions. Build exactly what's described here.

## What We're Building

A clean, minimal, English-language F1 dashboard — a single-page site that gives F1 fans a quick overview of the current season. No news clutter, no ads, no paywalls. Just the data that matters.

**Target audience:** Global F1 fans who want standings, results, and schedule at a glance.

## Operations — Hermes AI Agent

The live site is **managed by an AI agent called Hermes** (the data pipeline). This split is intentional:

| Layer | Owner | What it does |
| --- | --- | --- |
| **Frontend** | You (Cursor) / humans | `index.html`, `css/style.css`, `js/main.js` — static HTML/CSS/JS |
| **Data** | **Hermes** (AI agent) | Fetches F1 data, writes `/data/*.json` and optional `summary.md`, commits & pushes to the Git repo |
| **Hosting** | GitHub Pages | Serves the repo as static files — no server, no build step |

**Hermes responsibilities:**
- Update `data/standings.json`, `data/schedule.json`, `data/results.json` after each race / on schedule
- Optionally write `data/summary.md` (AI race summary)
- Push changes to the Git repository

**Hermes does NOT touch:** frontend HTML/CSS/JS (unless explicitly asked).

**Frontend data loading:** `main.js` fetches JSON with `{ cache: 'no-cache' }`, so Hermes updates appear on the next page load without cache-busting the data files. Asset cache-busting (`?v=N` on CSS/JS in `index.html`) is only needed when the frontend code changes.

## Architecture (CRITICAL)

This is a fully static site with zero backend:

```
/data/*.json   ← Hermes writes these (NOT your job)
/index.html    ← You build this (static HTML/CSS/JS)
/js/main.js    ← Fetches /data/*.json and renders everything
/css/style.css
```

The site fetches JSON files from `/data/` at runtime using `fetch()`. You do NOT hardcode any data. The JSON files are generated by Hermes and pushed to the repo alongside your frontend. GitHub Pages serves everything as static files.

**Your job:** Build the HTML, CSS, and JS that reads those JSON files and renders a beautiful dashboard.

## Hard Constraints

1. **No frameworks.** Vanilla HTML, CSS, and JavaScript only. No React, Vue, Svelte, Tailwind, Bootstrap, etc.
2. **No build step.** No npm, no webpack, no bundler. Just plain files that work when opened or served.
3. **GitHub Pages compatible.** Everything must work when served from a static file server. No server-side code.
4. **Mobile-first responsive.** Must look great on phones, tablets, and desktop.
5. **Dark theme.** F1 aesthetic — dark backgrounds, bold typography, accent colors.
6. **Single page.** All sections on one scrollable page. Anchor links for navigation.

## Pages / Sections

The page has 6 sections stacked vertically (Upcoming Race is **above** Standings):

### 1. Hero / Header (sticky)
- Brand mark (inline SVG speedometer, NOT the official F1 logo) + "F1 Dashboard" title
- Current season year (e.g. "2026 Season")
- Navigation: anchor links to Upcoming, Standings, Results, Calendar
- Last updated timestamp (from `standings.json`, relative time)

### 2. Upcoming Race
- Race name, round number, Sprint badge if applicable
- Circuit name, city, country (no flag emojis)
- Live countdown to race start (client-side JS, updates every second)
- Caption: "until lights out · [UTC time]" → "LIGHTS OUT 🏁 / Race in progress" at T-0
- **Weekend schedule:** Qualifying and Race only — dates/times in **UTC** (English weekday, 24-hour, labeled `UTC`)

### 3. Season Standings
Two tables side-by-side (stack on mobile):

**Driver Standings:** Pos | Driver | Team | Pts | Wins  
**Constructor Standings:** Pos | Team | Pts | Wins

- Position column: plain numbers (1, 2, 3…) — no medal emojis
- Driver cell: full name + 3-letter code — **no nationality flags**
- Team-color left accent on each row + mini relative points bars
- "Last updated: Round X" subtitle

### 4. Latest Race Results
- Race name, date, circuit in subtitle
- Full results table: Pos | Driver | Team | Time/Status | Grid | Fastest Lap
- Position: numbers only (DNF styled for retirees)
- Driver: name + code, no flags
- Winner row: subtle gold background highlight; 🏆 trophy before finish time in Time column
- Grid → finish position-change indicator (▲ / ▼ / ▬)
- DNF status in distinct red style

### 5. Season Calendar
| Rnd | Date | Race | Circuit | Winner |

- No country flag emojis
- Completed races: green tint + checkmark on winner
- Next upcoming race: red left accent + "Next" tag
- Winner column: name or TBD

### 6. Race Summary (optional)
- AI-generated Markdown from `/data/summary.md`
- Section hidden if file missing (404)

## Design / Aesthetic

Inspiration: Clean dashboards (Stripe, Linear, Vercel) meets F1 broadcast graphics.

**Color palette**

| Token | Value | Use |
| --- | --- | --- |
| Background | `#0a0a0a` | Page |
| Surface/Cards | `#1a1a1a` / `#1e1e1e` | Cards, tables |
| Text primary | `#f0f0f0` | Body |
| Text secondary | `#a0a0a0` | Muted |
| Accent | `#e10600` | F1 red |
| Gold | `#ffd700` | Winner row, Sprint badge |
| DNF | `#ff4444` | Retirements |
| Completed race | `#1a3a1a` | Calendar done rows |

**Typography:** System font stack (`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif`). Section titles uppercase. Table numbers: tabular-nums.

**Layout:** Max width 1200px, section padding 60px, card radius 8px, subtle `#2a2a2a` borders.

**Responsive:** Mobile single column; standings 2-col at 768px+; results Grid column hidden on small screens.

## JSON Data Structures

These are the EXACT JSON shapes the frontend fetches. Handle these structures precisely.

### `/data/standings.json`
```json
{
  "season": "2026",
  "round": "7",
  "updated": "2026-06-18T12:00:00Z",
  "driverStandings": [
    {
      "position": "1",
      "driver": {
        "code": "ANT", "givenName": "Andrea Kimi", "familyName": "Antonelli",
        "nationality": "Italian", "permanentNumber": "12"
      },
      "constructor": "Mercedes",
      "points": "156", "wins": "5"
    }
  ],
  "constructorStandings": [
    {
      "position": "1", "constructor": "Mercedes",
      "nationality": "German", "points": "262", "wins": "6"
    }
  ]
}
```

### `/data/schedule.json`
```json
{
  "season": "2026",
  "races": [
    {
      "round": "1", "raceName": "Bahrain Grand Prix",
      "date": "2026-03-15", "time": "15:00:00Z",
      "circuit": {
        "circuitName": "Bahrain International Circuit",
        "location": { "locality": "Sakhir", "country": "Bahrain" }
      },
      "firstPractice": { "date": "2026-03-13", "time": "11:30:00Z" },
      "secondPractice": { "date": "2026-03-13", "time": "15:00:00Z" },
      "thirdPractice": { "date": "2026-03-14", "time": "12:30:00Z" },
      "qualifying": { "date": "2026-03-14", "time": "16:00:00Z" },
      "sprintQualifying": null,
      "sprint": null,
      "winner": null
    }
  ]
}
```
*(Schedule JSON may include FP/Sprint sessions; the UI only displays Qualifying + Race.)*

### `/data/results.json`
```json
{
  "season": "2026", "round": "7",
  "raceName": "Monaco Grand Prix", "date": "2026-05-31",
  "circuit": {
    "circuitName": "Circuit de Monaco",
    "location": { "locality": "Monte Carlo", "country": "Monaco" }
  },
  "results": [
    {
      "position": "1", "grid": "2",
      "driver": { "code": "ANT", "givenName": "Andrea Kimi", "familyName": "Antonelli", "nationality": "Italian" },
      "constructor": "Mercedes", "laps": "78", "time": "1:42:13.847",
      "status": "Finished",
      "fastestLap": { "rank": "1", "lap": "72", "time": "1:16.234" }
    }
  ]
}
```

## File Structure (in the repo)

```
/
├── index.html
├── CLAUDE.md          ← this file (agent instructions)
├── css/style.css
├── js/main.js
├── data/              ← Hermes writes these
│   ├── standings.json
│   ├── schedule.json
│   ├── results.json
│   └── summary.md
├── assets/
│   ├── favicon.ico
│   └── og-image.png
├── CNAME              ← optional custom domain
└── README.md
```

## JavaScript Requirements

`main.js` must:

1. `fetch()` all 3 JSON files from `/data/` in parallel on page load (`cache: 'no-cache'`)
2. Error handling: missing file → graceful message, don't break the page
3. Countdown timer: live countdown to next race, update every second
4. **Session times in UTC** — `fmtUTC()` with `timeZone: 'UTC'`, English weekday, 24-hour, `UTC` suffix
5. DNF highlighting: non-"Finished" status → distinct style
6. Smooth scrolling for anchor links
7. "Last updated" display: relative time from `standings.json`
8. Zero external dependencies

**Do NOT implement (removed by design):**
- Nationality / country flag emojis
- Podium medal emojis (🥇🥈🥉) — use plain position numbers
- Championship-leader badge in the hero
- Full FP1/FP2/FP3/Sprint schedule in the UI (Qualifying + Race only)

## SEO

- `<title>`: "F1 Dashboard 2026 — Standings, Results & Schedule"
- `<meta name="description">`: clean description
- Open Graph + Twitter Card tags
- JSON-LD structured data (WebSite)
- Semantic HTML

## Analytics

GoatCounter before `</body>`:

```html
<script data-goatcounter="https://YOUR_CODE.goatcounter.com/count"
        async src="//gc.zgo.at/count.js"></script>
```

## What You Do NOT Build

- Backend, API, database
- Data fetching from Ergast (Hermes does that)
- Git automation for data updates (Hermes does that)
- Auth, user accounts, CMS

## Development Notes

- Sample JSON in `/data/` is for local dev; Hermes overwrites in production
- Run locally: `python -m http.server 8000` → http://localhost:8000 (needs a server for `fetch()`)
- Bump `?v=N` on CSS/JS links in `index.html` when frontend assets change (browser cache)
- Test mobile (375px), tablet (768px), desktop (1440px)

## Pending Tasks (TODO)

- [x] **Favicon** — `assets/favicon.ico` (+ `favicon.png`, `apple-touch-icon.png`). Custom speedometer mark on dark bg with red accent — NOT the official F1 logo.
- [x] **OG / social image** — `assets/og-image.png` (1200×630). Matches site aesthetic (red glow, speed-lines, speedometer mark).
- [ ] **CNAME** — only if a custom domain is chosen.
- [ ] **Live URLs in meta** — `canonical`, `og:url` and JSON-LD `url` still use the placeholder `https://f1dashboard.example/`. On deploy, set them to the real URL (`https://<username>.github.io/<repo>/` or the custom domain). For reliable social previews also make `og:image` / `twitter:image` absolute URLs.
- [x] **GoatCounter** — live site code `f1dashboard2026` set in `index.html` (`https://f1dashboard2026.goatcounter.com`).

### Deferred design ideas (optional)
- [ ] Season Calendar as card grid on desktop
- [x] Skeleton loaders instead of "Loading…" text — shimmer placeholders shown before data loads
- [ ] Display/racing font for headings (external font → trade-off vs. zero-dependencies)

## Implementation Status (current build)

Fully built and runnable. Vanilla HTML/CSS/JS, no build step, no external dependencies.

**Files:** `index.html`, `css/style.css`, `js/main.js`, `data/{standings,schedule,results}.json`, `data/summary.md`, `assets/{favicon.ico,favicon.png,apple-touch-icon.png,og-image.png}`, `README.md`

**Section order (top → bottom):**
1. Hero/header (sticky) — brand, nav, last-updated
2. **Upcoming Race** — above Standings
3. Season Standings (drivers + constructors)
4. Latest Race Results
5. Season Calendar
6. Race Summary (optional; hidden if `data/summary.md` missing)

**UI decisions (current):**
- **Minimal driver display:** name + 3-letter code only — no flag emojis anywhere
- **Plain position numbers** in standings and results — no podium medals
- **No championship-leader badge** in the hero
- **Weekend schedule:** Qualifying + Race only; times in UTC (global audience)
- **Race winner:** gold row highlight + 🏆 in Time column
- Team-color accent border on standings/results rows (`TEAM_COLORS` map)
- Mini relative points bars in standings tables
- Hero gradient + diagonal speed-lines texture
- Grid→finish position-change indicators in results
- Micro-interactions: row hover, section fade-in (IntersectionObserver), smooth transitions; disabled under `prefers-reduced-motion`
- **Countdown:** compact tabular-nums digits, blinking `:` separators, red glow; switches to "LIGHTS OUT 🏁" at T-0
- **Skeleton loaders:** shimmering placeholder rows/banner shown before data loads (`showSkeletons()` in `main.js`, `.skeleton*` in CSS); shimmer disabled under `prefers-reduced-motion`
- **Brand assets:** custom speedometer mark (NOT the official F1 logo) — `favicon.ico/.png`, `apple-touch-icon.png`, and 1200×630 `og-image.png`
- **Hero brand mark:** inline SVG speedometer (gauge arc + needle) in `.hero__mark`, white on red accent — replaced the previous italic "F1" text mark (which resembled the official F1 logo)
- **Analytics:** GoatCounter live (`f1dashboard2026`); privacy-friendly, no cookies, ignores `localhost`

**Recent cleanup (no dead code):**
- Removed unused flag maps (`FLAGS`, `COUNTRY_FLAGS`, `driverFlag`, `countryFlag`)
- Removed unused CSS (`hero__leader`, `pos--medal`, `.flag`, `--silver`, `--bronze`)
- Removed unused `results` parameter from `renderCalendar()`
